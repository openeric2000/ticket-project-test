# 🧩 專題開發環境安裝教學（Tailwind CSS 4 + Node.js）

## 📦 1. 安裝 Node.js
Tailwind 需要透過 Node.js 進行編譯。  
請至官網下載並安裝最新版（建議 v18 以上）：

🔗 [Node.js 官方下載](https://nodejs.org/zh-tw/download)

安裝完成後，在終端機（或 VSCode 的 Terminal）輸入以下指令檢查版本：
```bash
node -v
npm -v
```
若能顯示版本號，代表安裝成功。

---

## ⚠️ PowerShell 執行 `npm` 時出現錯誤？
若出現以下紅字錯誤：
```
npm : 因為這個系統上已停用指令碼執行，所以無法載入 C:\Program Files\nodejs\npm.ps1 檔案。
```
代表 PowerShell 預設禁止執行指令碼（出於安全性設計）。

---

## ✅ 臨時啟用（建議使用，安全又不會改系統設定）
1. 以系統管理員身分開啟 PowerShell  
   （在開始功能表搜尋「PowerShell」→ 右鍵 → 以系統管理員身分執行）
2. 輸入以下指令並按下 Enter：
   ```powershell
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```
3. 出現提示時輸入 `Y`，然後再試：
   ```bash
   npm -v
   node -v
   ```
   若能顯示版本號即成功。

> 💡 這個設定只在**目前這一個 PowerShell 視窗**生效。  
> 關閉視窗後會自動恢復成原本的安全設定。  
> 每次開新視窗時若要使用 npm，再輸入一次即可。  

---

## 🚫 不建議的永久修改
由於某些系統（例如學校電腦或公司筆電）有群組原則鎖定，  
永久修改會出現錯誤訊息：
```
無法設定您的執行原則，因為該設定已被更高層級的原則覆寫。
```
因此請**維持使用上方的臨時啟用方式**，不需永久開啟。

---

## 🧱 2. 專案初始化與套件安裝
**第一次 clone 專案後，請執行以下步驟：**
```bash
# 進入專案資料夾
cd ticket-project-test

# 安裝所有依賴套件（包含 Tailwind）
npm install
```

成功後會在專案中出現 `node_modules` 資料夾。

---

## 🎨 3. 啟動 Tailwind 監聽編譯
Tailwind 會把 HTML 裡用到的 class 轉成 CSS。  
執行以下指令開始自動編譯：
```bash
npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch
```
🟢 若終端機顯示：
```
Rebuilding...
Done in Xms.
```
代表成功。保持終端機開著即可，Tailwind 會即時更新樣式。

---

## 🗂️ 4. 檔案結構（範例）
```
ticket-project-test/
│
├─ src/
│   └─ input.css           # Tailwind 的輸入檔（不要刪）
│
├─ dist/
│   └─ output.css          # Tailwind 編譯後的結果（自動生成）
│
├─ tailwind.config.js      # Tailwind 設定檔
├─ package.json            # npm 設定與版本資訊
└─ index.html / MainPage.html ...
```

---

## ⚠️ 5. 注意事項
- 若畫面沒有樣式 → 檢查是否有執行 `npx tailwindcss`。
- 不要手動修改 `dist/output.css`（會被自動覆蓋）。
- 推上 GitHub 時，可在 `.gitignore` 裡加入：
  ```
  node_modules/
  dist/output.css
  ```
  讓每個人自己在本地端重新編譯。

---

## ✅ 6. 每次開專案流程（簡化啟動方式）

每次開機或重新啟動 VS Code 後，請執行以下步驟：

1. 開啟 PowerShell（或 VS Code 終端機）  
2. 臨時允許 PowerShell 執行 npm：
   ```powershell
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```
3. 進入專案資料夾：
   ```bash
   cd C:\Users\你的帳號\source\AZURE\ticket-project-test
   ```
4. 啟動 Tailwind：
   ```bash
   npm run dev
   ```

---

## 🧠 7. 如何設定「npm run dev」快速指令

在你的 `package.json` 中加入以下區塊（和 `devDependencies` 平行）：
```json
"scripts": {
  "dev": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
}
```

設定完成後，只要輸入：
```bash
npm run dev
```
就能啟動 Tailwind，即時編譯並監聽變化。  
不需要再輸入那一長串指令 🎉
